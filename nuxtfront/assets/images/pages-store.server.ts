/*import { usePagesStore } from '@/stores/pages.ts';

export default defineNuxtPlugin(async (nuxtApp) => {
    const pagestore = usePagesStore(nuxtApp.vueApp.$nuxt.$pinia);
    await useAsyncData("pages", () => pagestore.fetchPages());
});*/